spring:
  application:
    name: train-service
  
  datasource:
    url: jdbc:postgresql://${DB_SERVER:10.100.33.184}:${DB_PORT:5432}/${DB_DATABASE:ticketing_app}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:P@ssw0rd}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: ${TRAIN_SERVICE_PORT:8084}

logging:
  level:
    com.train_service: INFO
    org.springframework.web: INFO