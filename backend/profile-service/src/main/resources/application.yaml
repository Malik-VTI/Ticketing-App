spring:
  application:
    name: profile-service
  datasource:
    url: jdbc:postgresql://${DB_SERVER:10.100.33.184}:${DB_PORT:5432}/${DB_DATABASE:ticketing_app}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:P@ssw0rd}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: ${PROFILE_SERVICE_PORT:8090}
